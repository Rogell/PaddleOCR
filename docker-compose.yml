version: "3.8"

services:
  run:
    build: .
    volumes:
      - .:/project
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              device_ids:
                - "1"
              capabilities: [gpu]
