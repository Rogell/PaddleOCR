version: "3.8"

services:
  trainer:
    build: .
    volumes:
      - .:/project
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              device_ids:
                - "1"
              capabilities: [gpu]
